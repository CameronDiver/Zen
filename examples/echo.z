fn main(argc: int, argv: [string]) -> int {
	let i = 1;

	while i < argc {
		if i > 1 {
			printf(" %s", argv[i]);
		} else {
			printf("%s", argv[i]);
		}
		i = i + 1;
	}

	printf("\n");
	return 0;
}
